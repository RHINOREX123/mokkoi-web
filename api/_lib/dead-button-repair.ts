import type { ComponentNode } from '../../src/types/mokkoi'
import type { RouteGraph } from './planner'

export function repairDeadButtons(
  tree: ComponentNode,
  _routeGraph: RouteGraph,
  _screenLabel: string
): ComponentNode {
  // Phase 0 stub — returns tree unchanged.
  // Agent 3 implements full repair logic.
  return tree
}
